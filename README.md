# ServerTestNotification

A basic Node.js project scaffolded in the `Server` workspace.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run the project:
   ```bash
   npm start
   ```

The app starts a simple HTTP server on port `3000` by default.
